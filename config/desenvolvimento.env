# 🚀 Configuração de Desenvolvimento - Talent Sphere Registry
# ⚠️  IMPORTANTE: Este arquivo não deve conter segredos!
# 🔐 Todos os segredos estão no Doppler

# Supabase - Homologação (para desenvolvimento)
VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}

# Ambiente
VITE_ENVIRONMENT=development
VITE_NODE_ENV=development

# Configurações de Debug
VITE_DEBUG_MODE=true
VITE_LOG_LEVEL=debug

# APIs de IA (configuradas no Doppler)
VITE_TOGETHER_API_KEY=${VITE_TOGETHER_API_KEY}
VITE_GROQ_API_KEY=${VITE_GROQ_API_KEY}

# Configurações de Aplicação
VITE_APP_NAME="Talent Sphere Registry"
VITE_APP_VERSION="1.0.0"

# Configurações de Deploy
DEPLOY_TARGET=desenvolvimento
BASE_URL=/

# Configurações de Proxy (apenas para desenvolvimento)
VITE_USE_PROXY=true
VITE_PROXY_TARGET=https://zbiivgtdamejiwcabmcv.supabase.co 